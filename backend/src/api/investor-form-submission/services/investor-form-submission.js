'use strict';

/**
 * investor-form-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::investor-form-submission.investor-form-submission');
