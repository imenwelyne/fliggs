'use strict';

/**
 * investor-form-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::investor-form-submission.investor-form-submission');
